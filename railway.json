{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python3 manage.py migrate && python3 manage.py collectstatic --noinput && gunicorn mysite.wsgi --bind 0.0.0.0:$PORT"
    },
    "environments": {
        "production": {
            "buildCommand": "pip install -r requirements.txt"
        }
    }
}